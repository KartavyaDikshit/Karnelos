[package]
name = "mkimage"
version = "0.1.0"
edition = "2021"

[build-dependencies]
bootloader = { version = "0.11", default-features = false, features = ["bios"] }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
